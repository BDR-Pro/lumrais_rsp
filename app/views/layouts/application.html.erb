<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Lumrais Rsp" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>
    
    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <!-- Navbar -->
<header class="bg-white shadow">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4 flex justify-between items-center">
    <div class="flex items-center space-x-6">
      <a href="/" class="text-xl font-bold text-indigo-600 hover:text-indigo-800">Lumrais RSP</a>

      <nav class="hidden md:flex space-x-4 text-sm text-gray-700">
        <%= link_to "VPN", class: "hover:underline" %>
        <%= link_to "Download", download_index_path, class: "hover:underline" %>
        <%= link_to "Quick Run", quick_run_index_path, class: "hover:underline" %>
        <%= link_to "Infra Action", infra_action_index_path, class: "hover:underline" %>
      </nav>
    </div>

    <% if user_signed_in? %>
      <div class="flex items-center space-x-4 text-sm text-gray-700">
        <%= link_to "Profile", profile_path, class: "hover:underline" %>
        <%= link_to "Payments", payments_path, class: "hover:underline" %>
        <% if current_user.seller? %>
          <%= link_to "Withdraw", seller_withdraws_path, class: "hover:underline" %>
        <% end %>
        <span><%= current_user.email %></span>
        <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "text-red-500 hover:underline" %>
      </div>
    <% else %>
      <%= link_to "Login", new_user_session_path, class: "text-sm text-indigo-600 hover:underline" %>
    <% end %>
  </div>
</header>

  <body>
    <%= yield %>
  </body>
</html>
